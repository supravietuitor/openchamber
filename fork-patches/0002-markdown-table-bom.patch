diff --git a/packages/ui/src/components/chat/markdown/decorate.ts b/packages/ui/src/components/chat/markdown/decorate.ts
index 51efc32c4..4234d96a6 100644
--- a/packages/ui/src/components/chat/markdown/decorate.ts
+++ b/packages/ui/src/components/chat/markdown/decorate.ts
@@ -499,7 +499,7 @@ export const decorateMarkdown = (root: HTMLElement, ctx: DecorateContext): void
 // ---------------------------------------------------------------------------
 
 const downloadBlob = (filename: string, content: string, mime: string): void => {
-  const blob = new Blob([content], { type: mime });
+  const blob = new Blob([(mime.startsWith('text/') ? '\uFEFF' : '') + content], { type: mime });
   const url = URL.createObjectURL(blob);
   const link = document.createElement('a');
   link.href = url;
